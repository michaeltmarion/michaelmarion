extends layout

block styles

block scripts
  link(rel='stylesheet', href='stylesheets/pages/blog.css')

block content
  h1 Latest Updates
  div#posts
    each post in posts
      a.transition(href='/blog/post/#{post.id}')
        h2 #{post.title}
        p !{post.snippet}
